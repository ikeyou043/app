<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>メモアプリ</title>
  </head>
  <body>
    <h1>メモ一覧</h1>
    <ul>
      <% @memos.each do |id, memo| %>
        <li>
          <a href="/memos/<%= id %>"><%= memo[:title] %></a>
        </li>
      <% end %>
    </ul>
  </body>
</html>